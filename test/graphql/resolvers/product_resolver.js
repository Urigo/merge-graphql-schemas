const queries = {
  products: () => {},
  product: () => {},
};

const mutations = {
  create_product: () => {},
  update_product: () => {},
};

const subQueries = {
  Product: {
    clients: () => {},
  }
};

export { queries, mutations, subQueries };
