const queries = {
  clients: () => {},
  client: () => {},
};

const mutations = {
  create_client: () => {},
  update_client: () => {},
};

const subQueries = {
  Client: {
    products: () => {},
  }
};

export { queries, mutations, subQueries };
